{% extends 'base.html.twig' %}

{% block body %}
<h1>{{ produit.nom }}</h1>
<p>{{ produit.description }}</p>
<p>{{ produit.prix }}€</p>
<p>{{ produit.qte }}</p>
<p>{{ produit.categorie.nom }}</p>

<h2>Modifier le produit</h2>
{{ form(form_edit) }}
{% endblock %}